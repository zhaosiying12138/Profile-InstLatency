# 综合验收章 + 实验缺口补齐 + 博客方法论分层重构

## 已确认决策
基线=**generic-rv64**（+mattr=+v）；新章**插为第六章**并**重构第五章为 6 个分层小节**；演示程序=**手写 IR intrinsics**；口径=**周期数+百分比**。

## A. 补齐三处实验缺口（先行，数据供新章引用）
1. **E7 全 36 对**：`gen_asm_v2.py` 已生成 with_replacement(8,2)=36 对（现仅跑 10）→ `run_batch.py` 跑全量 m1（m4 已有）→ `profile_final.py` 增加逐对 slope 表 + parallel/serial 判定矩阵（对角自证）。
2. **E5 WAW 补对照**：生成器改为每个 d 值都带 ctrl 孪生；只补跑缺失 ctrl（~200 个，~12 分钟）→ WAW 转折点提取（最后一个有停顿的 d 的总间距）填满 8 指令×3 LMUL 表。
3. **E11 复合可加性**：新脚本 `scripts/e11_composite.py`——单程序含多个 marker 分段（独立流/RAW 链/跨类对/WAW 对，标量 drain 分隔），解析期望=各段 yxmodel 公式之和，逐点 assert 实测==期望；输出 PASS 表（既是缺口补齐，也是新第六章的"机器行为可加"背书）。
4. 文档诚实化：附录 A 明确两句——"计划中的层 2 源码补丁被纯配置方案取代（零补丁优于打补丁）"、"多 agent 并行在强串行依赖下仅用于侦察，主体单线推进"。

## B. 端到端综合测试（新第六章核心）
1. **`tests/demo.ll`**：手写 RVV intrinsics——3~4 深 vdivu 依赖链 + 6+ 条独立 vmul/vadd/vredsum 填充（m1/e32）。验收标准：两版 llc 辱出**顺序差异 ≥3 条指令**且 gem5 周期差 ≥5%；不达标则加大链深/填充数迭代（最多 3 轮）。
2. **`scripts/build_demo.sh` + `scripts/mark_region.py`**：
   - `llc -O2 -mcpu=YuShuXinV2` vs `llc -O2 -mcpu=generic-rv64 -mattr=+v,+zvl256b` 两版 .s（generic 若拒绝 intrinsics 则按预案换 rocket 基线并在文中注明）；
   - 自动在函数体前后注入 `__yx_marker_*` 标号（复用既有 marker 机制）；
   - 两版各自 汇编→链接→gem5 实跑→Δ；
   - 两版各自 `llvm-mca -mcpu=YuShuXinV2` Total Cycles → 与 gem5 实测做对齐小表；
   - `--timeline -iterations=1` 取 YuShuXinV2 版的 overlap 视图。
3. **四张真机截图**（紫色 PowerShell，走既有流水线，加入 `make_shot_cmds.sh`/`make_all_shots.sh`）：
   - `shot_ab_asm`：两版汇编顺序对比（diff 视图）
   - `shot_ab_gem5`：两个 ELF 同窗实跑 + 两个 Δ
   - `shot_ab_mca`：mca 两版周期 + gem5 对齐表
   - `shot_overlap`：timeline 双发射/流水重叠视图
4. **叙事**：以"一次编译看全程"串联——vdivu 非流水 12 拍（第二章设计）→ 调度器用我们的模型把独立运算填进除法气泡（第三/五章的 Latency/R 值）→ gem5 实测周期下降 X%（差分方法第四章）→ mca 预测与 gem5 对齐（模型正确性）→ timeline 展示 overlap（IssueWidth=2/双管先验的兑现）。

## C. 博客重构（方法论分层，非实验报告）
1. **第五章重构为 6 节**（3 详 3 简）：
   - 5.1 前提与公理（E0+E8 合并，简，shot_e0）
   - 5.2 吞吐：独立流反演 R（E2+E9 拟合与涌现并入，详，shot_e2 + stream_nscan/design_vs_measured.svg）
   - 5.3 延迟：链与矩阵两路合围（E3+E4 合并，详，shot_e3/e4 + matrix_heat/latency_m1.svg）
   - 5.4 依赖的另外两个面貌：WAW 与 WAR（E5+E6 合并，简，补齐后的 WAW 表+阴性 WAR；E7 全对数据并入下一节）
   - 5.5 结构参数：管线归属与发射宽度（E7 36 对 + E8，简，shot_e7；全表进证据矩阵）
   - 5.6 门禁与工具闭环（gate+mca，详，shot_gate/shot_mca + mca_vs_gem5.svg）
   - 章首加\"阅读地图\"段说明详略分层逻辑；E10 一段话并入 5.5 或 5.6（访存为辅线）
2. **新第六章\"综合测试：让编译器用这台机器的语法写作\"**（详写，4 截图，B-4 叙事）
3. **原六/七/八 → 七/八/九**（脚本化重编号+交叉引用修正）；证据矩阵补 4 行（E5 表、E7 36 对、E11、综合测试 A/B）；Highlights 加 1 条端到端结论；附录 B 加 demo 复现命令。
4. **README**：中英各加一小节\"End-to-end case study\"（A/B 周期数+百分比+mca 对齐一句话）。

## D. 收尾
重跑 `make_all_shots.sh`（旧图保持、新图 4 张紫底≥80% 且文本核验）→ `build_blog_html.py` → `verify_timing_exactness.py` 仍全绿 → git commit（\"ch6 end-to-end case study + gap fills + layered restructure\"）→ SSH push → 记忆更新（完成态+demo 数字）。

## 验收清单
- E7 36/36 有判定；WAW 表非空；E11 PASS；gate ok≥23 bad=0
- 两版编译顺序可视频率差异；gem5 Δ 差≥5%；mca 与 gem5 对齐（误差≤2 拍/段）
- 博客 9 章、第五章 6 节、第六章 4 截图；HTML 重建；README 双语含 case study；远端新 commit 可见

## 风险预案
generic-rv64 不收 V intrinsics→换 rocket（注明）；调度重排不足→加深依赖链/填充数；timeline 过宽→-iterations=1；E5 补跑量大→限 m1 全量+m2/m4 关键点。